[pytest]
# 테스트 케이스 파일명/함수명 패턴 설정
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 터미널 출력 스타일 설정
addopts = -v --tb=short

# 기본 테스트 경로 설정
testpaths = 
    tests

; # (선택) 경고 무시
; filterwarnings =
;     ignore::DeprecationWarning